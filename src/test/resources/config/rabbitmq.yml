development:
  mq_uri: "amqp://localhost:5672"
  preservation:
    destination: "dev-queue"
  dissemination:
    destination: "dev-queue???"

test:
  mq_uri: "amqp://localhost:5672"
  preservation:
    destination: "test-queue"
  dissemination:
    destination: "test-queue???"

production:
  mq_uri: "amqp://localhost:5672"
  preservation:
    destination: "prod-queue"
  dissemination:
    destination: "prod-queue???"

